import { createContext } from "react";


/** React context to hold collections data, which will be viewed and/or accessed throughout the
 * app.
 */
const CollectionsContext = createContext(null);

export default CollectionsContext;
